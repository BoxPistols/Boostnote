# The Boosters depends on packages with conflicting peer ranges
# (e.g. @enyaxu/markdown-it-anchor wants markdown-it@8, project uses @6).
# Default to legacy peer resolution so `npm install` / `ni` work without flags.
# Proper fix = bump markdown-it during the dependency-modernization phase.
legacy-peer-deps=true
